# PRD: 로컬 음성-텍스트 변환 도구 (macOS)

## 1. 프로젝트 개요

**제품명**: QuickSTT (가칭)

**목적**: 마이크 입력을 빠르고 정확하게 텍스트로 변환하여 클립보드에 자동 복사하는 가벼운 macOS 데스크톱 도구. M365/OneNote 받아쓰기의 무거움과 불안정성을 대체한다.

**타겟 사용자**: 회의록, 메모, 보고서 작성 시 음성 입력을 활용하고 싶은 사무직 사용자(개발자 본인, 1인 사용 우선).

**핵심 가치**: 가볍다, 빠르다, 오프라인 동작, 한국어 정확도 높음.

## 2. 기술 스택

언어는 Python 3.10 이상을 사용한다. 음성 인식 엔진은 faster-whisper(모델은 medium, 한국어 우선)를 쓰고, 오디오 캡처는 sounddevice 라이브러리를 사용한다. 단축키는 pynput으로 글로벌 핫키를 등록하고, 클립보드는 pyperclip으로 처리한다. 알림은 macOS 기본 osascript 또는 plyer를 사용한다. 트레이 아이콘은 1차 개발에서는 제외하고 CLI/백그라운드 스크립트로 시작한다.

## 3. 기능 요구사항 (MVP)

**FR-1. 글로벌 단축키로 녹음 시작/종료**
사용자가 어떤 앱에서 작업 중이든 `Cmd+Shift+R`을 누르면 녹음이 시작되고, 다시 누르면 녹음이 종료된다. 녹음 중에는 터미널에 상태가 표시되고, macOS 알림으로 "녹음 시작/종료"를 알린다.

**FR-2. 음성 → 텍스트 변환**
녹음 종료 시 자동으로 faster-whisper(medium 모델, 언어=ko)로 변환을 수행한다. 변환 진행 중에는 "변환 중..." 알림을 표시한다.

**FR-3. 클립보드 자동 복사**
변환 완료된 텍스트를 클립보드에 자동 복사하고, "변환 완료 (N자)" 알림을 띄운다. 사용자는 어떤 입력창에서든 Cmd+V로 붙여넣을 수 있다.

**FR-4. 로그 저장**
변환 결과를 `~/QuickSTT/logs/YYYY-MM-DD.txt`에 타임스탬프와 함께 누적 저장한다. 나중에 검색/재활용 가능하도록.

**FR-5. 설정 파일**
`~/QuickSTT/config.yaml`에서 모델 크기(tiny/base/small/medium/large-v3), 언어, 단축키, 로그 저장 여부를 변경할 수 있다.

## 4. 비기능 요구사항

녹음 시작 단축키 입력부터 실제 녹음 시작까지 0.5초 이내여야 한다. 1분 분량 음성을 medium 모델 + Apple Silicon(M1/M2/M3) 환경에서 30초 이내에 변환해야 한다(CoreML/Metal 가속 활용). 백그라운드 대기 상태에서 메모리 사용량은 200MB 이하, CPU 사용률은 1% 이하여야 한다. 녹음 가능 최대 길이는 10분(설정 변경 가능).

## 5. 디렉토리 구조

```
quickstt/
├── main.py              # 진입점, 단축키 리스너
├── recorder.py          # 오디오 녹음 모듈
├── transcriber.py       # faster-whisper 래퍼
├── notifier.py          # macOS 알림
├── config.py            # 설정 로드
├── requirements.txt
├── config.yaml.example
└── README.md
```

## 6. 개발 단계 (단계별 검증)

**Phase 1**: 환경 셋업 및 단순 변환 테스트. 5초짜리 wav 파일을 입력받아 텍스트로 변환되는지 확인한다(transcriber.py 단독 테스트).

**Phase 2**: 마이크 녹음 → 임시 wav 저장 → 변환 → 콘솔 출력. 단축키 없이 Enter 키로 시작/종료하는 단순 버전.

**Phase 3**: 글로벌 단축키 통합 (`Cmd+Shift+R`). 백그라운드 실행 가능 상태로.

**Phase 4**: 클립보드 복사 + macOS 알림 추가.

**Phase 5**: 로그 저장, 설정 파일 분리, README 작성.

## 7. macOS 특이사항

macOS는 마이크 접근과 입력 모니터링(글로벌 단축키)에 권한이 필요하다. 첫 실행 시 시스템 설정에서 두 가지 권한을 허용해야 한다. 터미널(또는 IDE)에 마이크 권한과 손쉬운 사용 > 입력 모니터링 권한을 부여하는 절차를 README에 명시한다. Apple Silicon이라면 faster-whisper는 자동으로 CoreML/Metal을 활용하지 않으므로, 더 빠른 성능을 원하면 `mlx-whisper` 또는 `whisper.cpp`를 대안으로 검토한다(Phase 6 이후 고려).

## 8. 완료 정의 (Definition of Done)

MVP는 다음 시나리오가 동작하면 완료로 간주한다. 사용자가 메모 앱이나 Slack을 켜둔 상태에서 `Cmd+Shift+R`을 누르고 30초간 한국어로 말한 뒤 다시 단축키를 누르면, 약 10~20초 내에 알림이 뜨고 Cmd+V로 정확한 텍스트가 붙여넣어진다.

## 9. 향후 확장 (MVP 이후)

이후 확장 아이디어로는 메뉴바 트레이 아이콘, 실시간 스트리밍 변환(말하는 동시에 표시), 변환 후 LLM으로 자동 정리(말투 다듬기, 회의록 포맷팅), 화자 분리(pyannote), 자주 쓰는 용어 사전 등록(회사 고유명사 정확도 개선) 등이 있다.

---